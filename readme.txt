create :
	2dValue  : gives an array with dim and initialize with value
	till 4 dimension 
	Random : create array with min-max, random
	till 4 dim
	readFromFiles : 
	till 4 dimension
	(Image Reading)
	createFromFunction : computes the values based on indices of the array

Modify :
	transpose :
	setBoundary Value:
		1D : 2 values(functions) left, right
		2D : 4 values left, right, top bottom
		3D : 6 values  left, right, top bottom, back front
	PrintMatrix
		for all dimensions(1D, 2D, 3D, 4D)
	WriteMatrix
		for all dimensions(1D, 2D, 3D, 4D)

Compute :
	number of iteration(iterate till some value)
	time bound (iterate till some time)
	changeBound (iterate till delta is less/greater than some value)
	type of computation(avarage, min, max, median.......)(separate function for all)
	number neighbours